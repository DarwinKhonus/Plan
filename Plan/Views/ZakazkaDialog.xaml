<Window x:Class="Plan.Views.ZakazkaDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:Plan.ViewModels"
        d:DataContext="{d:DesignInstance Type=vm:ZakazkaEditViewModel}"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{Binding Titulek}"
        Width="420"
        SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        FocusManager.FocusedElement="{Binding ElementName=NazevBox}">

    <Window.Resources>
        <Style TargetType="TextBlock" x:Key="Popisek">
            <Setter Property="Margin" Value="0,0,0,4" />
            <Setter Property="Foreground" Value="#4A5563" />
        </Style>
    </Window.Resources>

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,14">
            <TextBlock Text="Název zakázky" Style="{StaticResource Popisek}" />
            <TextBox x:Name="NazevBox"
                     Padding="6,5"
                     Text="{Binding Nazev, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" />
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="14" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Termín od" Style="{StaticResource Popisek}" />
                <DatePicker SelectedDate="{Binding DatumOd, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" />
            </StackPanel>

            <StackPanel Grid.Column="2">
                <TextBlock Text="Termín do" Style="{StaticResource Popisek}" />
                <DatePicker SelectedDate="{Binding DatumDo, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" />
            </StackPanel>
        </Grid>

        <TextBlock Grid.Row="2"
                   Margin="0,0,0,14"
                   TextWrapping="Wrap"
                   Foreground="#C0392B"
                   Text="{Binding ChybovaZprava}">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Visible" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding JePlatny}" Value="True">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Zrušit"
                    MinWidth="88"
                    Padding="10,6"
                    IsCancel="True"
                    Margin="0,0,8,0" />
            <Button Content="Uložit"
                    MinWidth="88"
                    Padding="10,6"
                    IsDefault="True"
                    Click="Ulozit_Click"
                    IsEnabled="{Binding JePlatny}" />
        </StackPanel>
    </Grid>
</Window>
